<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BackMeUp">

    <Style TargetType="local2:ResetToDefaultButton" xmlns:local2="using:BackMeUp.UI.Components">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local2:ResetToDefaultButton">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="local2:GroupPanel" xmlns:local2="using:BackMeUp.UI.Components">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local2:GroupPanel">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
