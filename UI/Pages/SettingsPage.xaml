<Page
    x:Class="BackMeUp.UI.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:BackMeUp.Data.Models"
    mc:Ignorable="d">

    <Grid>
        <ListView 
            x:Name="SettingsListView" ItemsSource="{x:Bind _configs.GameSaveConfigs}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:GameSaveConfig">
                    <StackPanel>
                        <TextBlock Text="{x:Bind Game}" />
                        <TextBlock Text="{x:Bind SavePath}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
